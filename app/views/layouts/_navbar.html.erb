<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/products">Anime Store</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" aria-current="page" href="/products">Action Figures</a></li>
        <% if current_user.has_role?('Admin') || current_user.has_role?('Staff') %>
          <li class="nav-item"><a class="nav-link" href="/users">Users</a></li>
        <% end %>
        <li class="nav-item"><a class="nav-link" href="/users/edit">Logout</a></li>
      </ul>
    </div>
    <% if current_user %>
      <% @cart = current_user.cart || current_user.create_cart %>
      <% if @cart && @cart.line_items.any? %>
      <a href="<%= cart_path(@cart) %>" class="custom-cart-icon"><i class="fas fa-shopping-cart"></i></a>
      <% end %>
    <% end %>
  </div>
</nav>
