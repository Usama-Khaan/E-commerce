<h1><%= @product.title %></h1>
<p><%= @product.description %></p>

<%= button_to "Edit", edit_product_path(@product), method: :get, class: "edit-button" %>
<%= button_to "Delete", product_path(@product), method: :delete, data: {
  turbo: true,
  confirm: "Are you sure?"
} %>
